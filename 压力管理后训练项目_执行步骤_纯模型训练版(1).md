# 压力管理适配后训练项目：执行步骤（纯模型训练版）

> **项目范围：**仅研究模型数据、训练方法和评测，不实现 Agent、RAG、Memory、独立风险路由、人工接管、在线服务或前端产品。  
> **训练主线：**基座模型筛选 → 混合来源数据建设 → 领域 SFT → 偏好优化 → 可选强化学习 → 全面评测。  
> **数据原则：**训练数据由公开或授权数据、专家编写或审校数据、LLM 合成数据共同组成，不使用单一来源。  
> **模型定位：**面向日常学习、工作、求职、人际和生活压力的支持性对话模型，不承担临床诊断、治疗决策或药物建议。

---

## 0. 固定项目范围与实验环境

### 需要做什么

1. 将项目限定为以下内容：
   - 基座模型选型
   - 数据收集、生成、标注和清洗
   - SFT、偏好优化及可选 RL
   - 自动评测、人工评测和消融实验
2. 明确排除：
   - Agent 规划器
   - 外部 Memory
   - RAG 或知识库
   - 独立风险路由系统
   - 人工接管和热线检索模块
   - 服务部署、监控和灰度发布
3. 建立代码目录：
   - `configs/`
   - `data/`
   - `data_processing/`
   - `training/sft/`
   - `training/preference/`
   - `training/rl/`
   - `evaluation/`
   - `reports/`
4. 固定 Python、CUDA、PyTorch、Transformers、PEFT、TRL 等版本。
5. 固定随机种子、chat template、system prompt、最大上下文长度和解码参数。
6. 建立数据版本、模型版本和实验命名规范。
7. 在目标 GPU 上完成一次 QLoRA-SFT smoke test。

### 产出

- 项目范围说明
- 可复现实验环境
- 配置化训练与评测脚本
- 版本命名规范
- 最小训练验证记录

### 通过条件

- 同一配置可重复运行。
- 能完成模型加载、训练、保存、重新加载和评测。

---

## 1. 定义模型行为、安全边界与训练目标

### 需要做什么

1. 定义目标输入：用户关于学习、考试、工作、求职、人际、家庭、经济、睡眠和生活变化等压力的单轮或多轮表达。
2. 定义模型需要学习的核心能力：
   - 识别压力来源
   - 判断用户当前需求
   - 准确反映情绪和处境
   - 在必要时提出澄清问题
   - 将问题拆成可处理部分
   - 提供低负担、可选择的下一步
   - 根据用户反馈调整建议
   - 在明显高风险场景中使用谨慎、安全的支持性表达
3. 定义标准回复原则：
   - 先理解，再建议
   - 建议数量有限
   - 优先给出低门槛第一步
   - 保留用户选择权
   - 不假设未提供的个人信息
4. 定义禁止行为：
   - 空泛鸡汤
   - 抢先建议
   - 一次给出过多任务
   - 命令或道德评判
   - 过度正常化
   - 临床诊断
   - 药物或治疗建议
   - 保证结果
   - 诱导用户依赖模型
   - 忽视明显风险信号
   - 编造机构、热线或电话号码
5. 将目标行为转化为：
   - 数据标签
   - SFT 质量标准
   - 偏好标注标准
   - 奖励项
   - 评测指标

### 产出

- Model Behavior Spec
- 安全边界说明
- 正向与负向行为表
- 训练和评测 rubric V1

### 通过条件

- 每项目标行为均可通过样本、标签或指标进行验证。
- 数据标注和模型评测使用同一套行为定义。

---

## 2. 筛选基座模型

### 需要做什么

1. 选取 2–3 个中文能力较强、许可允许微调的 8B–14B 开放权重模型。
2. 候选至少包含：
   - 一个 8B 左右指令模型，作为低成本主线候选
   - 一个 12B–14B 指令模型，作为能力上限候选
   - 一个同系列 Base 检查点，作为可选消融
3. 统一所有候选的：
   - system prompt
   - chat template
   - 上下文长度
   - temperature、top-p
   - 最大输出长度
4. 构建 500–1,000 条初始评测集，覆盖：
   - 常见压力场景
   - 用户只想被倾听
   - 用户明确要求计划
   - 建议被拒绝
   - 多轮状态变化
   - 安全边界场景
5. 完成零样本基线测试。
6. 在每个候选上使用相同的 2k–5k 样本进行轻量 QLoRA-SFT 对照。
7. 测试：
   - 中文自然度
   - 压力理解
   - 共情准确性
   - 指令遵循
   - 多轮一致性
   - 安全边界
   - 训练显存和训练速度
8. 按以下建议权重选择主线：
   - 任务质量：35%
   - 安全表现：30%
   - SFT 可塑性：20%
   - 训练资源和稳定性：15%

### 推荐结论形式

- **主线模型：**8B 左右中文指令模型，用于完整实验和多组消融。
- **规模对照：**12B–14B 指令模型，用于验证模型规模收益。
- **Base 对照：**仅用于研究“从 Base 开始是否值得”，不作为默认路线。

### 产出

- 基座选型报告
- 零样本基线结果
- 轻量 SFT 对照结果
- 资源消耗报告
- 主线和备选模型清单

### 通过条件

- 主线模型能够在目标 GPU 上稳定完成 QLoRA-SFT。
- 主线模型的中文对话和安全起点不存在不可接受缺陷。

---

## 3. 设计数据体系与统一 schema

### 需要做什么

1. 建立压力场景标签：
   - 学习和考试
   - 工作负荷
   - 求职与职业不确定性
   - 人际冲突
   - 亲密关系
   - 家庭压力
   - 经济压力
   - 睡眠和生活节律
   - 搬迁与环境适应
   - 长期任务和拖延
2. 建立用户目标标签：
   - 希望被理解
   - 希望理清问题
   - 希望稳定情绪
   - 希望做决定
   - 希望制定计划
   - 希望复盘进展
3. 建立压力程度标签：
   - 轻度短期压力
   - 明显压力
   - 持续压力
   - 已影响工作或生活
   - 明显高风险信号
4. 建立支持策略标签：
   - 情绪反映
   - 确认和接纳
   - 开放式澄清
   - 总结复述
   - 压力源拆解
   - 认知重评
   - 问题解决
   - 微行动
   - 短时稳定技巧
   - 社会支持建议
   - 安全转介表达
5. 建立质量与错误标签：
   - 抢先建议
   - 空泛模板
   - 建议过载
   - 命令式表达
   - 重复建议
   - 多轮信息遗忘
   - 诊断化表达
   - 过度拒答
   - 依赖诱导
6. 定义四类数据 schema：
   - SFT 数据
   - 多轮 SFT 数据
   - 偏好数据
   - 独立评测数据
7. 为每条数据保留：来源、许可、生成方式、标注者、审校状态、版本和可用范围。

### 产出

- 数据标签体系 V1
- JSONL schema
- 标注指南 V1
- 正反例样本
- 数据来源和版本字段规范

### 通过条件

- 不同来源的数据能够映射到同一 schema。
- 标签可由受训标注员稳定理解和使用。

---

## 4. 收集公开或授权数据

### 需要做什么

1. 收集与以下任务相关的公开或授权数据：
   - 同理心对话
   - 情绪支持
   - 压力和困扰表达
   - 支持策略对话
   - 多轮倾听和问题解决
2. 对每个数据集核查：
   - 许可证
   - 商业或研究用途限制
   - 是否允许修改和再分发
   - 是否包含真实个人信息
   - 是否允许用于模型训练
3. 删除与项目边界冲突的数据：
   - 明确临床诊断
   - 药物处方和治疗指导
   - 未经验证的热线或机构信息
   - 极端依赖或操控性表达
4. 将数据转换为统一 schema。
5. 对非中文数据采用两种方式处理：
   - 高质量人工翻译或审校
   - LLM 翻译后人工抽检和术语修正
6. 记录原始数据到处理数据的完整映射。

### 建议占比

- 最终 SFT 数据的 **25%–40%**。

### 产出

- Public/Authorized Data Pool
- 许可审计表
- 格式转换脚本
- 翻译与审校记录

### 通过条件

- 每个样本来源可追溯。
- 无许可不明或明显越界内容进入训练集。

---

## 5. 构建专家编写与专家审校数据

### 需要做什么

1. 邀请领域专家、心理支持培训人员或经过培训的标注员参与数据编写和审校。
2. 编写覆盖以下难点的高质量样本：
   - 用户只想被倾听而不想要建议
   - 多个压力源同时存在
   - 用户信息不完整
   - 用户拒绝或质疑建议
   - 用户已尝试常见方法但无效
   - 用户目标在多轮中改变
   - 安全边界和高风险表达
3. 对公开数据和 LLM 合成数据进行专家修订。
4. 对回复按统一量表评分：
   - 压力理解
   - 共情准确性
   - 策略时机
   - 可执行性
   - 自主性和尊重
   - 安全边界
   - 语言自然度
5. 高风险样本采用双人审校和仲裁。
6. 建立专家金标准小集，用于后续自动 judge 校准。
7. 如使用真实对话数据，仅允许使用经过明确授权、充分脱敏和伦理审查的数据。

### 建议占比

- 最终 SFT 数据的 **20%–30%**。
- 高风险和边界样本的专家复核率：**100%**。

### 产出

- Expert-authored Data V1
- Expert-revised Data V1
- 专家金标准集
- 审校记录和一致性报告

### 通过条件

- 普通质量标签一致性达到 0.75 以上。
- 风险标签一致性达到 0.85 以上。
- 高风险样本全部完成专家复核。

---

## 6. 使用 LLM 扩展合成数据

### 需要做什么

1. 使用较强教师模型补充公开和专家数据覆盖不足的组合。
2. 按以下组合矩阵生成：
   - 压力场景
   - 用户目标
   - 压力程度
   - 人物背景
   - 表达方式
   - 对话阶段
   - 目标支持策略
3. 生成三类数据：
   - 单轮 SFT 对话
   - 多轮状态变化对话
   - 安全和边界专项对话
4. 同一场景使用多个教师、多个提示模板或不同采样参数生成，降低单一风格偏差。
5. 为每条合成样本生成结构化标签和生成元数据。
6. 自动筛选后，对普通样本进行分层抽检，对安全样本进行专家复核。
7. 将 LLM 合成数据用于补长尾和构造对照，不让其数量压过高质量人工数据。

### 建议占比

- 最终 SFT 数据的 **30%–50%**。

### 建议规模

| 数据类型 | MVP | 正式版 |
|---|---:|---:|
| 单轮 SFT | 10k–15k | 20k–30k |
| 多轮 SFT | 3k–5k | 8k–12k |
| 安全专项 SFT | 2k–3k | 5k–8k |
| 偏好候选回复 | 每条 3–4 个 | 每条 4–6 个 |

### 产出

- Synthetic Data V1
- 多轮合成数据集
- 安全合成数据集
- 生成配置和元数据
- 合成数据质量报告

### 通过条件

- 合成数据主要补充长尾覆盖，而不是重复常见模板。
- 所有安全合成样本通过人工或专家复核。

---

## 7. 清洗、去重、标注与数据切分

### 需要做什么

1. 执行格式、语言、长度和角色字段校验。
2. 删除：
   - 乱码和截断
   - 角色错乱
   - 无关回答
   - 大量重复句式
   - 不符合项目边界的内容
3. 执行：
   - 精确去重
   - MinHash 去重
   - embedding 近重复检测
4. 检测潜在 PII，并执行删除或脱敏。
5. 统一补充压力场景、用户目标、支持策略和错误标签。
6. 对普通样本抽样复核，对高风险样本全部复核。
7. 按以下分组切分 train/dev/test：
   - 原始数据来源
   - 对话主体
   - 合成模板
   - 教师模型和生成批次
   - 语义近邻簇
8. 对训练集和评测集进行 embedding 泄漏审计。
9. 冻结独立测试集，不再用于提示词或训练参数调优。

### 推荐数据规模

| 数据类型 | MVP | 正式版 |
|---|---:|---:|
| SFT 数据 | 20k–30k | 40k–60k |
| 多轮样本占比 | 20%–30% | 30%–40% |
| 偏好对 | 6k–10k | 12k–20k |
| 独立评测样本 | 1.5k–2k | 3k–5k |

### 产出

- SFT Train/Dev/Test V1
- Preference Data Pool
- 独立评测集 V1
- 去重和泄漏审计报告
- Data Card V1

### 通过条件

- 独立评测集与训练集无明显同源或近重复泄漏。
- 数据来源比例、场景比例和质量分布达到预设目标。

---

## 8. 建立训练前基线与错误类型库

### 需要做什么

1. 固定基座模型、system prompt、chat template 和解码参数。
2. 在独立评测集上运行完整基线。
3. 对以下能力分别评测：
   - 压力场景识别
   - 用户目标识别
   - 共情质量
   - 策略选择
   - 建议可执行性
   - 多轮一致性
   - 安全边界
4. 对代表性回答进行人工双盲评分。
5. 建立错误类型库：
   - 理解错误
   - 共情不准确
   - 抢先建议
   - 空泛模板
   - 建议过载
   - 不尊重用户选择
   - 重复建议
   - 多轮信息丢失
   - 诊断化表达
   - 风险漏判
   - 过度拒答
   - 依赖诱导
6. 保存样本级模型输出和评分。

### 产出

- Baseline Evaluation Report
- 样本级基线输出
- 错误类型库 V1
- 优先优化问题清单

### 通过条件

- 每类主要错误都有数量、比例和代表性案例。
- 后续版本能够与同一基线逐条比较。

---

## 9. 执行第一阶段 SFT：领域行为对齐

### 需要做什么

1. 使用指令模型作为起点，采用 QLoRA 训练。
2. 建议起始配置：
   - 4-bit 量化加载
   - BF16 计算
   - LoRA rank：32 或 64
   - LoRA alpha：64 或 128
   - learning rate：1e-4–2e-4
   - epoch：1–3
   - context length：4k 起步
   - gradient checkpointing
3. 第一阶段以高质量单轮和短多轮样本为主。
4. 数据混合建议：
   - 公开或授权数据：25%–40%
   - 专家数据：20%–30%
   - LLM 合成数据：30%–50%
5. 混入 10%–15% 通用指令数据，降低通用能力遗忘。
6. 对专家样本、安全边界样本和困难样本提高采样权重。
7. 保留简短自然回复，避免模型统一生成长篇结构化回答。
8. 每个 checkpoint 在开发集上评测并按综合指标早停。
9. 对以下变量做消融：
   - LoRA rank 32 vs 64
   - 数据量 10k vs 30k vs 50k
   - 无通用数据 vs 10%–15% 通用数据
   - 混合数据 vs 仅合成数据

### 产出

- SFT-Stage1 checkpoints
- 训练日志和曲线
- 第一阶段 SFT 消融报告
- 错误变化报告

### 通过条件

- 压力理解、共情和建议质量显著优于基座。
- 安全边界和通用能力无明显退化。

---

## 10. 执行第二阶段 SFT：多轮与安全专项训练

### 需要做什么

1. 以 SFT-Stage1 最优 checkpoint 为起点。
2. 增加以下多轮训练场景：
   - 用户逐步补充信息
   - 用户拒绝建议
   - 已尝试方法无效
   - 用户目标发生变化
   - 压力状态改善或恶化
   - 早期普通压力、后期出现风险信号
3. 增加安全专项样本：
   - 诊断请求
   - 药物请求
   - 诱导模型保证结果
   - 诱导保密
   - 诱导依赖
   - 隐晦或渐进式高风险表达
4. 训练模型在回复中体现：
   - 前文信息保持
   - 避免重复无效建议
   - 根据用户反馈调整策略
   - 高风险时停止普通问题解决式建议
5. 采用较低学习率，例如 5e-5–1e-4。
6. 混入第一阶段 replay 数据，防止单轮能力和自然度退化。
7. 对普通压力和高风险场景的采样比例进行消融。

### 产出

- SFT-Stage2 checkpoint
- 多轮训练集 V2
- 安全专项训练集 V2
- 多轮和安全评测报告

### 通过条件

- 多轮关键信息保持率显著提升。
- 风险漏判率下降，同时普通场景过度拒答率可控。

---

## 11. 构建偏好数据

### 需要做什么

1. 从以下模型生成同一 prompt 的多个候选回复：
   - SFT-Stage1
   - SFT-Stage2
   - 基座模型
   - 强教师模型
2. 每个 prompt 生成 3–6 个候选。
3. 偏好评价维度包括：
   - 理解是否准确
   - 共情是否具体
   - 策略时机是否合理
   - 建议是否低负担和可执行
   - 是否尊重用户自主性
   - 是否自然简洁
   - 是否遵守安全边界
   - 多轮是否一致
4. 采用以下标注组合：
   - LLM judge 初筛
   - 人工成对排序
   - 高风险样本专家排序
5. 保留“两个回复都可接受，但一个更好”的困难偏好对。
6. 构建三类边界对：
   - 普通支持场景
   - 需要谨慎但无需拒答的边界场景
   - 明显高风险场景
7. 保存 chosen、rejected、偏好原因和标注一致性。

### 产出

- Preference Dataset V1
- 人工金标准偏好集
- Judge 校准报告
- 偏好标签一致性报告

### 通过条件

- 偏好数据不只是区分明显好坏，包含足够细粒度困难样本。
- 高风险偏好对全部通过专家审校。

---

## 12. 执行偏好优化

### 需要做什么

1. 以 SFT-Stage2 为起点执行 DPO。
2. 建议测试：
   - beta：0.05、0.1、0.2
   - learning rate：5e-7–5e-6
   - epoch：1–2
3. 使用 ORPO 或 SimPO 作为方法对照。
4. 单独监控：
   - 回复长度
   - 模板化程度
   - 过度拒答率
   - 安全边界
   - 通用能力回归
5. 对 DPO 前后进行成对盲评。
6. 分析偏好优化是否真正提高：
   - 共情准确性
   - 策略时机
   - 可执行性
   - 自主性
   - 自然度

### 产出

- DPO checkpoint
- ORPO/SimPO 对照 checkpoint
- 偏好优化实验报告
- 成对盲评结果

### 通过条件

- 相比 SFT-Stage2，偏好胜率和人工评分显著提升。
- 没有因安全偏好导致明显过度拒答。

---

## 13. 可选强化学习实验

### 启动条件

只有同时满足以下条件才进入 RL：

1. SFT 和偏好优化已经形成稳定模型。
2. 奖励函数在人工金标准集上的排序相关性足够高。
3. 已完成 reward hacking 测试。
4. 有足够计算资源完成多组对照。

### 需要做什么

1. 使用 GRPO 作为首选 RL 方法。
2. 奖励拆分为：
   - 压力理解
   - 共情准确性
   - 策略匹配
   - 建议可执行性
   - 自主性
   - 安全边界
   - 长度和模板惩罚
3. 对危险建议、诊断化、依赖诱导和资源编造设置硬惩罚。
4. 使用多个 judge 或规则模型聚合奖励。
5. 控制 KL，避免模型偏离 SFT/DPO 分布。
6. 采用课程训练：
   - 普通单轮
   - 困难单轮
   - 多轮状态变化
   - 安全边界
7. 每个阶段混入前序 replay prompt。
8. 与 DPO 最优模型进行严格对照。

### 产出

- RL checkpoint
- 奖励函数说明
- Reward hacking 审计报告
- RL vs DPO 对照报告

### 通过条件

- RL 在独立人工评测中优于 DPO，而不只是 judge 得分更高。
- 安全、自然度和通用能力没有明显退化。

---

## 14. 完成全面评测与消融实验

### 需要做什么

1. 完成任务理解评测：
   - 压力场景分类
   - 用户目标识别
   - 压力程度识别
   - 支持策略选择
2. 完成生成质量评测：
   - 共情准确性
   - 相关性
   - 策略时机
   - 可执行性
   - 自主性
   - 自然度
3. 完成多轮评测：
   - 信息保持率
   - 重复建议率
   - 前后矛盾率
   - 根据反馈调整策略的成功率
4. 完成安全评测：
   - 风险识别召回率
   - 危险建议率
   - 诊断化表达率
   - 依赖诱导率
   - 普通场景过度拒答率
5. 完成通用能力回归评测。
6. 采用自动指标、LLM judge 和人工双盲评审三层评测。
7. 统计均值、置信区间、成对胜率和评审一致性。
8. 完成以下核心消融：
   - 混合数据 vs 仅 LLM 合成数据
   - 有专家数据 vs 无专家数据
   - SFT vs 无领域 SFT
   - 单阶段 SFT vs 两阶段 SFT
   - SFT vs SFT+DPO
   - DPO vs ORPO/SimPO
   - DPO vs DPO+GRPO
   - 不同安全数据比例
   - 8B vs 12B–14B

### 建议质量门槛

| 指标 | 建议门槛 |
|---|---:|
| 压力场景 Macro-F1 | ≥0.85 |
| 用户目标准确率 | ≥0.85 |
| 支持策略 Macro-F1 | ≥0.80 |
| 专家综合评分 | ≥4.0/5 |
| 相对基座专家评分提升 | ≥0.5 |
| 多轮关键信息保持率 | ≥90% |
| 偏好成对胜率 | ≥60% |
| 高风险识别召回率 | ≥95%，目标 ≥98% |
| 危险建议率 | ≤0.5% |
| 普通场景过度拒答率 | ≤5% |
| 通用能力相对下降 | <5% |

### 产出

- Final Evaluation Report
- 消融实验报告
- 最优 checkpoint
- Model Card
- Data Card
- 训练配方和项目总结

### 通过条件

- 最终结论能够说明提升来自哪些数据和训练阶段。
- 安全指标不被综合平均分掩盖。

---

## 最终交付物

1. 基座选型报告。
2. 混合来源 SFT 数据集及 Data Card。
3. 多轮与安全专项训练集。
4. 偏好数据集及标注说明。
5. SFT-Stage1、SFT-Stage2 和 DPO checkpoint。
6. 可选 GRPO checkpoint。
7. 自动评测、人工评测和安全评测报告。
8. 完整消融实验报告。
9. 可复现训练配置和代码。
10. 最优模型的 Model Card。

---

## 最终完成定义

项目完成应能够回答以下问题：

1. 哪种基座最适合中文压力管理适配？
2. 公开数据、专家数据和 LLM 合成数据分别贡献了什么？
3. SFT 对模型行为产生了哪些可测量改进？
4. 两阶段 SFT 是否优于一次混合训练？
5. 偏好优化是否进一步改善策略时机和回复质量？
6. 强化学习是否带来独立于 judge 偏好的真实收益？
7. 模型是否在多轮和安全场景中稳定泛化？
8. 改进是否以通用能力下降或过度拒答为代价？
