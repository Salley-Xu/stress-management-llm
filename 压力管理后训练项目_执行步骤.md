# 压力管理适配大模型后训练项目：执行步骤

> 目标模型：中文文本对话模型  
> 推荐主线：Qwen3.5-9B 后训练检查点 + QLoRA-SFT + DPO + 安全强化  
> 适用范围：日常压力管理、情绪支持、问题拆解和行动规划；不用于心理疾病诊断、治疗决策或药物建议。

---

## 0. 建立项目管理与复现环境

### 需要做什么

1. 建立代码仓库，划分 `data/`、`training/`、`evaluation/`、`serving/`、`configs/` 和 `reports/`。
2. 固定 Python、CUDA、PyTorch、Transformers、PEFT、TRL、vLLM/SGLang 等版本。
3. 接入实验追踪工具，记录模型版本、数据版本、随机种子、训练参数和评测结果。
4. 建立数据与模型版本命名规则。
5. 建立配置驱动的训练、评测和推理脚本。
6. 在目标 GPU 上完成一次最小化 QLoRA smoke test。

### 产出

- 可复现代码仓库
- 环境锁定文件
- 实验命名和版本规范
- 最小训练与推理脚本

### 通过条件

- 同一配置重复运行可得到一致结果。
- 能在目标硬件上完成数据加载、前向、反向、保存和推理。

---

## 1. 定义产品目标、场景和安全边界

### 需要做什么

1. 明确目标用户为面临学习、工作、求职、人际、家庭、睡眠和生活变化等日常压力的成年人。
2. 定义核心任务：
   - 倾听与情绪确认
   - 压力源澄清
   - 问题拆解
   - 短时稳定
   - 制定低负担行动
   - 复盘进展
   - 必要时建议寻求现实支持或专业帮助
3. 定义非目标：
   - 不做临床诊断
   - 不做心理测量结论
   - 不提供治疗或药物建议
   - 不鼓励用户依赖模型或远离现实关系
4. 建立风险等级：
   - L0：轻度、短期日常压力
   - L1：持续压力或已影响生活效率
   - L2：明显绝望、自伤暗示或严重功能受损
   - L3：明确计划、工具、时间或正在发生的紧急风险
5. 为每个风险等级定义允许行为、禁止行为和升级路径。
6. 编写统一的系统行为规范与人工升级规范。

### 产出

- 产品需求文档 PRD
- 能力边界说明
- 风险分级表
- Safety Spec
- 场景与任务分类表

### 通过条件

- 产品、算法和领域专家对边界达成一致。
- 每种风险等级均有明确、可测试的模型行为。

---

## 2. 设计系统架构和模型职责

### 需要做什么

1. 将系统拆分为：
   - 输入治理
   - 风险路由
   - 对话状态管理
   - 支持策略规划
   - 主生成模型
   - 输出安全检查
   - 日志与监控
2. 设计独立风险分类器或规则模块，不让生成模型单独承担危机识别。
3. 定义对话状态字段：压力源、严重度、用户目标、已尝试方法、偏好、禁忌和当前阶段。
4. 定义支持策略集合及策略组合规则。
5. 将热线、机构和紧急资源设置为外部配置或检索结果，禁止模型自由生成联系方式。
6. 设计人工接管、异常回退和版本回滚机制。

### 产出

- 系统架构图
- 模块接口定义
- 对话状态 schema
- 策略标签 schema
- 风险路由规则

### 通过条件

- 高风险输入可绕过普通生成流程并进入专用处理链路。
- 主模型失效时仍有安全回退响应。

---

## 3. 完成基座模型筛选

### 需要做什么

1. 选取候选：
   - Qwen3.5-9B 后训练检查点
   - Qwen3.5-9B-Base
   - Qwen3-14B 或其他稳定中文模型作为备选
   - 较大模型作为教师模型和评审模型
2. 为所有候选统一 chat template、系统提示、解码参数和上下文长度。
3. 构建 500–1,000 条基线开发集，覆盖普通压力、持续压力、高风险、拒答边界和多轮场景。
4. 完成零样本基线测试。
5. 对 9B 后训练检查点与 Base 检查点使用同一批 5k 高质量数据完成轻量 SFT 对照。
6. 在目标 GPU 上测试训练显存、推理显存、吞吐、延迟和工具链稳定性。
7. 按“安全 40% + 任务质量 35% + 工程可行性 25%”选择主线模型。

### 产出

- 基座选型报告
- 零样本基线结果
- 轻量 SFT 对照结果
- 工程性能报告

### 通过条件

- 主线模型能够在目标硬件稳定训练和部署。
- 主线模型在安全和中文对话质量上没有不可接受缺陷。

---

## 4. 建立数据分类体系和数据规范

### 需要做什么

1. 定义压力领域标签：学习、考试、工作负荷、求职、职业不确定、人际、亲密关系、家庭、经济、健康与睡眠、迁移适应、照护压力等。
2. 定义严重度标签：轻度、明显、持续、功能受损、高风险、紧急。
3. 定义用户目标标签：被理解、理清问题、快速稳定、做决定、制定计划、寻求资源、复盘进展。
4. 定义支持策略标签：情绪反映、确认接纳、开放式澄清、总结、压力源拆解、认知重评、问题解决、微行动、grounding、社会支持、资源转介和随访。
5. 定义禁止模式：空泛鸡汤、抢先建议、过度正常化、诊断化、保证结果、道德评判、依赖诱导、忽视风险、重复索取隐私。
6. 定义 SFT、偏好、安全和评测数据的 JSONL schema。
7. 编写标注指南、正反例和争议处理规则。

### 产出

- 标签体系 V1
- 数据 schema
- 标注指南 V1
- 正反例样本集

### 通过条件

- 标注员可依据指南独立完成标签和质量评分。
- 所有训练与评测数据均可使用统一 schema 表示。

---

## 5. 收集、生成和清洗训练数据

### 需要做什么

1. 收集公开数据，并逐项核查许可、用途限制和内容质量。
2. 组织领域专家或受训标注员编写高质量原创对话。
3. 在获得合法同意的前提下收集真实匿名对话，并进行最小化保留。
4. 使用教师模型按“人物画像 × 压力领域 × 严重度 × 对话阶段 × 策略路径”生成合成对话。
5. 生成安全红队数据，包括：
   - 隐晦或渐进式高风险信号
   - 诊断请求
   - 药物请求
   - 依赖诱导
   - 要求保密
   - 提示注入
   - 资源幻觉诱导
6. 执行自动清洗：
   - PII 检测与脱敏
   - 精确去重
   - MinHash 去重
   - embedding 近重复检测
   - 低质量文本过滤
   - 格式和长度校验
   - 诊断、药物和危机标签预筛
7. 保留每条数据的来源、许可、处理记录、标注版本和使用范围。

### 建议规模

| 数据类型 | MVP | 正式版 |
|---|---:|---:|
| SFT 数据 | 15k | 35k–50k |
| 偏好对 | 8k | 15k–25k |
| 安全/红队数据 | 4k–6k | 8k–12k |
| 独立评测数据 | 1.5k | 3k–5k |

### 产出

- 原始数据池
- 清洗数据池
- 数据来源清单
- 脱敏和去重报告
- Data Card 初稿

### 通过条件

- 所有数据来源可追溯。
- 训练数据中不存在明显 PII、无许可内容或高比例近重复样本。

---

## 6. 完成标注、审校和数据切分

### 需要做什么

1. 对普通样本标注压力领域、严重度、用户目标、策略序列和禁止模式。
2. 对高风险样本进行双人标注和专家复核。
3. 对回复质量按以下维度评分：
   - 压力理解
   - 共情准确性
   - 策略时机
   - 可执行性
   - 自主性与尊重
   - 安全边界
   - 语言自然度
   - 多轮一致性
4. 计算 Cohen κ 或 Krippendorff α。
5. 对分歧样本进行仲裁，并更新标注指南。
6. 按来源、模板、对话主体、语义近邻和场景分组切分 train/dev/test。
7. 对训练集和评测集执行 embedding 相似度泄漏审计。
8. 冻结独立评测集，禁止继续用于训练提示调优。

### 质量门槛

- 策略标签一致性 ≥0.75
- 风险标签一致性 ≥0.85
- 高风险样本专家复核率 100%
- 评测集近重复泄漏率为 0

### 产出

- SFT 数据 V1
- Safety 数据 V1
- 独立评测集 V1
- 标注质量报告
- 数据切分与泄漏审计报告

### 通过条件

- 标注一致性达到门槛。
- 独立评测集与训练集不存在同源或近重复泄漏。

---

## 7. 建立训练前基线和错误类型库

### 需要做什么

1. 冻结基座模型版本、系统提示和解码参数。
2. 在独立评测集上完成自动评测。
3. 对代表性样本进行双盲人工评审。
4. 完成至少 100 个 8–20 轮脚本测试。
5. 建立错误类型：
   - 压力源理解错误
   - 情绪识别不准确
   - 抢先建议
   - 空泛或模板化
   - 建议负担过高
   - 重复建议
   - 多轮状态丢失
   - 错误正常化
   - 风险漏检
   - 过度拒答
   - 依赖诱导
   - 诊断化表达
   - 隐私追问
   - 资源幻觉
6. 保存所有基线输出，作为后续版本回归参照。

### 产出

- Baseline Evaluation Report
- 基线输出集
- 错误类型库 V1
- 优先修复问题清单

### 通过条件

- 训练前各项指标和主要失败模式均被量化。

---

## 8. 执行领域 SFT

### 需要做什么

1. 使用 QLoRA 进行训练，建议起始配置：
   - 4-bit 量化
   - BF16 计算
   - LoRA rank 32 和 64 两档
   - 学习率 1e-4–2e-4 起步
   - 1–3 epoch
   - 上下文长度先 4k，后测试 8k
2. 将单轮与多轮样本按约 4:6 混合。
3. 将普通数据、高风险数据和通用指令数据分层采样。
4. 混入 10%–20% 通用指令数据。
5. 对高质量专家样本和安全边界样本增加采样权重。
6. 保留一定比例的简短自然回复样本。
7. 对以下设计做消融：
   - 后训练检查点 vs Base
   - 5k / 15k / 35k 数据量
   - 无策略标签 vs 单策略 vs 多策略序列
   - LoRA rank 32 vs 64
8. 每个 checkpoint 运行开发集评测并按安全和质量指标早停。

### 产出

- SFT-V1/V2 adapter
- 训练日志与曲线
- SFT 消融报告
- 新增和残留错误清单

### 通过条件

- 任务质量显著优于基座。
- 安全指标和通用能力没有明显退化。

---

## 9. 加强多轮状态、策略规划和个性化

### 需要做什么

1. 将对话历史转成结构化状态摘要。
2. 对比三种输入方式：
   - 完整历史
   - 结构化摘要
   - 完整历史 + 结构化摘要
3. 增加 8–20 轮长对话数据。
4. 增加用户反馈后需要调整策略的场景。
5. 增加信息反转和风险逐步升级场景。
6. 训练模型先选择策略序列，再生成自然语言回复。
7. 加入用户偏好和禁忌约束，例如简短、不要鸡汤、先听后建议。
8. 评测状态字段保持率、重复建议率、矛盾率和策略推进合理性。

### 产出

- 多轮训练集 V2
- 对话状态模块
- SFT-Multiturn 模型
- 多轮评测报告

### 通过条件

- 关键状态字段保持率 ≥90%。
- 重复建议和前后矛盾显著低于基座。

---

## 10. 构建偏好数据并执行偏好优化

### 需要做什么

1. 对同一上下文生成 3–5 个候选回复。
2. 由人工或多 judge 按统一量表排序候选。
3. 构建 chosen/rejected 对，并记录偏好原因。
4. 确保偏好数据覆盖：
   - 共情准确性
   - 策略时机
   - 可执行性
   - 自主性
   - 安全边界
   - 简洁自然
   - 多轮一致性
5. 加入“两个回答都可接受但一个更好”的困难偏好对。
6. 加入正常支持、边界场景和高风险场景三类拒答偏好对。
7. 以 DPO 为主线，测试 β=0.05/0.1/0.2。
8. 使用 ORPO 作为训练成本对照。
9. 单独监控过度拒答、回复长度和模板化程度。

### 产出

- Preference Dataset V1
- DPO 模型
- ORPO 对照模型
- 偏好优化实验报告

### 通过条件

- 专家盲评和成对胜率提升。
- 可执行性与自然度提升，且过度拒答率不恶化。

---

## 11. 执行安全强化和红队测试

### 需要做什么

1. 使用安全 SFT 强化风险识别、边界表达和升级话术。
2. 对安全偏好对执行高权重 DPO。
3. 训练或校准独立风险分类器。
4. 对以下场景开展多轮红队：
   - 隐晦和渐进式高风险信号
   - 明确紧急风险
   - 要求模型保密
   - 角色扮演绕过
   - 诱导诊断
   - 诱导药物建议
   - 诱导依赖
   - 提示注入
   - 请求虚假热线或机构
5. 校准高风险阈值，优先保证召回率。
6. 为普通压力场景建立 over-refusal 对照集。
7. 配置输出安全检查器和硬规则。
8. 对所有严重失败逐条修复并加入回归集。

### 安全硬门

- 高风险召回率 ≥98%
- 危险建议率 ≤0.5%
- 普通压力场景过度拒答率 ≤5%
- 紧急风险脚本逐条人工审阅通过
- 不自由生成热线、机构或电话号码

### 产出

- Safety-V1 模型
- 风险分类器
- 红队数据集
- 安全评测报告
- 安全回归集

### 通过条件

- 所有安全硬门通过。
- 不以普通回复质量抵消严重安全缺陷。

---

## 12. 完成全面评测、工程化和发布

### 需要做什么

1. 完成自动评测：
   - 压力领域分类
   - 严重度识别
   - 用户目标识别
   - 策略选择
   - 风险识别
   - 多轮状态跟踪
2. 完成双盲专家评审：
   - 共情
   - 相关性
   - 可执行性
   - 自主性
   - 自然度
   - 安全性
3. 完成多轮、鲁棒性、提示注入、公平性和通用能力回归测试。
4. 统计均值、胜率、置信区间和评审一致性。
5. 完成 4-bit/8-bit 量化，并重新运行完整评测。
6. 使用 vLLM 或 SGLang 完成服务化。
7. 接入风险路由、状态管理、输出审查、日志、告警和回滚。
8. 编写 Model Card、Data Card、限制说明和发布手册。
9. 按 R0→R1→R2→R3 分级发布：
   - R0：仅离线研究
   - R1：内部受控测试
   - R2：1%–5% 小流量灰度
   - R3：10%–30% 扩大灰度
10. 对灰度失败进行聚类、补数、小步再训练和回归验证。

### 发布门槛

| 维度 | 建议门槛 |
|---|---|
| 压力领域 Macro-F1 | ≥0.85 |
| 严重度加权 F1 | ≥0.80 |
| 用户目标准确率 | ≥0.85 |
| 专家质量均分 | ≥4.0/5，且较基线提升 ≥0.6 |
| 关键状态保持率 | ≥90% |
| 个性化配对胜率 | ≥65% |
| 通用能力回归 | 相对基座下降 <5% |
| 安全指标 | 必须全部通过安全硬门 |

### 产出

- MVP 模型与量化模型
- 推理服务
- 完整 Evaluation Report
- Model Card 与 Data Card
- 灰度和回滚方案
- 项目总结与下一阶段计划

### 通过条件

- 安全硬门、专家评审、工程 SLA 和回归测试全部通过。
- 监控、人工升级和版本回滚链路可用。

---

## 最终完成定义

项目完成不是模型“会安慰人”，而是同时满足以下条件：

1. 能准确识别压力场景、用户目标和当前对话阶段。
2. 能按照正确顺序执行倾听、澄清、稳定、拆解和行动规划。
3. 建议具体、低负担、可选择，不命令用户，也不制造依赖。
4. 多轮中能记住压力源、已尝试方案和用户偏好。
5. 普通压力场景不过度拒答。
6. 高风险场景停止普通建议并可靠进入专用升级流程。
7. 所有改进均可由独立评测集、专家盲评和版本回归复现。
